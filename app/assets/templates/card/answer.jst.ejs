<div class="answer container-fluid">

  <blockquote class="answer card-answer card-show quiz-show col-xs-12 panel-body"><%= card.escape('answer') %></blockquote>
  <div class="answer-buttons btn-toolbar btn-group btn-group-justified row">

    <div class="row">
      <div class="col-md-2">
        <button class="button btn-red btn-lg answer answer-button" id="48" type="submit" data-quality="0" data-card-id="<%= card.id %>" data-e-factor="<%= card.get('e_factor') %>" value="0">0</button>
      </div>
      <div class="col-md-2">
        <button class="button btn-red btn-lg answer answer-button" id="49" type="submit" data-quality="1" data-card-id="<%= card.id %>" data-e-factor="<%= card.get('e_factor') %>" value="1">1</button>
      </div>
      <div class="col-md-2">
        <button class="button btn-green btn-lg answer answer-button" id="50" type="submit" data-quality="2" data-card-id="<%= card.id %>" data-e-factor="<%= card.get('e_factor') %>" value="2">2</button>
      </div>
      <div class="col-md-2">
        <button class="button btn-green btn-lg answer answer-button" id="51" type="submit" data-quality="3" data-card-id="<%= card.id %>" data-e-factor="<%= card.get('e_factor') %>" value="3">3</button>
      </div>
      <div class="col-md-2">
        <button class="button btn-blue btn-lg answer answer-button" id="52" type="submit" data-quality="4" data-card-id="<%= card.id %>" data-e-factor="<%= card.get('e_factor') %>" value="4">4</button>
      </div>
      <div class="col-md-2">
        <button class="button btn-blue btn-lg answer answer-button" id="53" type="submit" data-quality="5" data-card-id="<%= card.id %>" data-e-factor="<%= card.get('e_factor') %>" value="5">5</button>
      </div>
    </div>

    <div class="row">
      <div class="col-md-2">
        <span>0: Nope.</span>
      </div>
      <div class="col-md-2">
        <span>1: Almost.</span>
      </div>
      <div class="col-md-2">
        <span>2: Tough, but got it.</span>
      </div>
      <div class="col-md-2">
        <span>3: Good.</span>
      </div>
      <div class="col-md-2">
        <span>4: Confident.</span>
      </div>
      <div class="col-md-2">
        <span>5: Easy.</span>
      </div>
    </div>

  </div>
</div>
